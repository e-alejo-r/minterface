package edg.android.minterface.rsize

/**
 * Enum class representing the measure type for window size.
 */
enum class Measure {
    /**
     * Represents the height measure.
     */
    HEIGHT,

    /**
     * Represents the width measure.
     */
    WIDTH
}
